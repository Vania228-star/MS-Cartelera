services:
  ms-cartelera:
    build: .
    container_name: ms-cartelera-container
    ports:
      - "8081:8081"
    environment:
      - DB_URL=${DB_URL}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - COGNITO_ISSUER_URI=${COGNITO_ISSUER_URI}
    restart: always